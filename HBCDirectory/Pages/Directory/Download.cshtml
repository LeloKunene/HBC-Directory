@page
@model HBCDirectory.Pages.Directory.DownloadModel
@{
    // This page returns a file — no HTML body is rendered.
    // The OnGetAsync handler returns File(...) which streams the PDF directly.
}
